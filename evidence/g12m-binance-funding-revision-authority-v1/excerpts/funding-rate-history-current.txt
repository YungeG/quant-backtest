Source: Binance Developer Docs — Get Funding Rate History
URL: https://developers.binance.com/docs/derivatives/usds-margined-futures/market-data/rest-api/Get-Funding-Rate-History
Receipt: mt31fw7a0cmufb
Acquisition form: readable search-provider extraction; not raw HTTP bytes
Scope: GET /fapi/v1/fundingRate request behavior and response schema

EXACT EXTRACTED PASSAGES

If `startTime` and `endTime` are not sent, the most recent 200 records are returned.

If the number of data between `startTime` and `endTime` is larger than `limit`, return as `startTime` + `limit`.

In ascending order.

`symbol` string Symbol. Example:BTCUSDT

`fundingRate` string Funding Rate. Example:-0.03750000

`fundingTime` integer·int64 Funding Time. Example:1570608000000

`markPrice` string mark price associated with a particular funding fee charge Example:34287.54619963

`rateType` string·enum Funding rate type. `Regular` for the normal funding rate; `Special` for the additional funding rate generated by stock dividends. Enum values: Regular Special Example:Regular

GET/fapi/v1/fundingRate

Example Responses
[
  {
    "symbol": "BTCUSDT",
    "fundingRate": "-0.03750000",
    "fundingTime": 1570608000000,
    "markPrice": "34287.54619963",
    "rateType": "Regular"
  }
]

OBSERVATION BOUNDARY

The extracted schema lists no revision ID, predecessor, corrected-at timestamp, first-publication timestamp, immutable row checksum, or as-of/vintage selector. This is a bounded observation of the extracted current schema.
