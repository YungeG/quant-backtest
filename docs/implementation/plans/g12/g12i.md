---
id: G12I
readiness: BLOCKED
gate_status: DRAFT
owner: market-bundle-builder validation
produces:
  - PriceStreamCoverageReport
  - MarketAvailabilityReport
  - RevisionProvenanceReport
consumes:
  - G12C MarketBundleManifest
  - G12G BarAggregationManifest
  - immutable purpose, availability, and revision closure declarations
depends_on:
  contract: [G12C, G12G]
  evidence: [real coverage declarations, G03 stale-policy projection parity]
  write_conflict: [builder-root-exports, builder-coverage-policy, acceptance-registry]
fan_out: [G12L-*, G12M-*]
---

# G12I Price, Availability, and Revision Coverage

## Outcome

Eventually produce exact price-purpose, market-availability, and revision-provenance reports from declared immutable closure evidence, without synthetic data or provider inference.

## Current status

`DRAFT / PARTIAL SOURCE-BOUNDED CLOSURE`. The Builder declaration value seam and G03 stale-policy parity are frozen. `G12I-TUSHARE-CN-A-SHARE-DAILY-PURPOSE-SCOPE-V1` PASSES one finite provider-specific EXECUTION_REFERENCE/VALUATION declaration. The [Tushare daily source-bounded v2 slice](g12i-tushare-cn-a-share-daily-source-bounded-v2.md) now passes D1-D4 for exact `000001.SZ`, XSHE, July-2026 Tushare `daily`/`trade_cal`/`suspend_d` evidence at source `4389877b8879fc9bb1a6d6544c4079a7d29312ab`. This does not close the general G12I analyzer or make a decision, live, or deployment claim.

Research authority: `docs/research/g12i-price-availability-revision-coverage.md`.

## Frozen prerequisite slice

The Builder now publicly owns:

1. `BuilderStaleMarkPolicy` and `PricePurposeRequirement`;
2. `MarketAvailabilityReason`, `AvailabilitySpan`, and `AvailabilityClosureDeclaration`;
3. `RevisionTerminalLineage` and `RevisionClosureDeclaration`.

These are passive, immutable, canonical schema-v1 declarations. They perform no resolution, inference, revision repair, provider access, or report generation. The static fixture proves deterministic shape only and carries no real-market completeness authority.

## Remaining blockers

For the general analyzer, other scopes, providers, aggregation cases, and general report surfaces remain unfrozen. The accepted Tushare v2 sub-slice closes one direct-provider-daily path with exact raw responses, receipt times, local hashes, missing-data classification, deterministic normalization/publication/replay, and failure precedence.

Under ADR 0008, unavailable Tushare permanent checksum, future-revision finality, correction lineage, and provider completeness are explicit limitations rather than blockers for that source-bounded path. Unknown open-day absence remains blocking. G12G remains authoritative when aggregation occurs; the one-to-one Tushare daily path must not mint a fake aggregation manifest merely to satisfy the older provisional seam.

## Provisional implementation seam

After real prerequisites pass, the general G12I analyzer may return all three reports atomically or one structured failure. The Tushare v2 subplan separately freezes one minimal provider-specific availability/revision observation report for future exact-artifact G12M binding; it does not freeze the general report surface.

## Readiness checks

Before changing `readiness` to `READY`, evidence must prove:

- immutable profile/build ownership of required purposes;
- exact stale-policy projection parity;
- real gap reason authority and complete non-overlapping declarations;
- real terminal revision closure and explicit empty semantics;
- no cross-purpose fallback or execution/liquidation forward fill;
- deterministic failures, atomicity, repeat hashes, and Builder-only imports.

## Non-goals

No provider truth inference, calendar derivation, gap invention, Bar generation, interpolation, revision repair, Runtime/Kernel production import, Engine branch, decision-grade claim, live use, or deployment authorization.
