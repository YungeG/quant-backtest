---
id: G12H
readiness: BLOCKED
gate_status: DRAFT
owner: market-bundle-builder validation
produces:
  - RuleCoverageReport
consumes:
  - G12C MarketBundleManifest
  - future immutable profile rule-coverage declaration
depends_on:
  contract: [G12C]
  evidence: [real profile rule-dimension declaration, normalized typed rule-source fixture]
  write_conflict: [builder-root-exports, builder-coverage-policy, acceptance-registry]
fan_out: [G12L-*, G12M-*]
---

# G12H Rule Coverage

## Outcome

Eventually prove complete, non-overlapping historical coverage for every rule dimension explicitly required by one immutable market-profile declaration, without duplicating Kernel economics.

## Current status

`DRAFT / BLOCKED`. The exact development-only prerequisite Bundle is `PASSED` at `832f53a74d3f74436ecae8672bd1c0dd3530c814`, but it proves that the five required dimensions have no common target coverage. Calendar, order rules, and corporate-action entitlements exact-cover July 2026; market fees and stamp duty stop in August 2023. The deterministic existing result remains `COVERAGE_GAP / market_fees`.

[ADR 0004](../../../adr/0004-official-rules-effective-until-authoritatively-superseded.md) now approves additive effective-until-authoritatively-superseded semantics and retrospective successor closure without backdating evidence. [The v2 plan](g12h-effective-until-superseded-v2.md) is `READY` for F1 evidence acquisition only. G12H remains blocked until exact predecessor, endpoint, successor-index, candidate-disposition, and closure evidence passes; F2 projection, F3 publication, and analyzer RED remain unauthorized.

Research authority:

- `docs/research/g12h-rule-coverage.md`;
- `docs/research/g12h-five-dimension-target-coverage-blocker-v1.md`;
- `docs/research/g12h-xshe-july-2026-fee-tax-authority-primary-sources.md`.

## Blocker to resolve first

Execute only F1 of the v2 plan: freeze verified official predecessor and post-target endpoint evidence, complete competent-authority successor-index evidence, deterministic candidate dispositions, and one canonical retrospective closure artifact for all four fee/tax lineages. Existing PASSED G08H and publication artifacts remain immutable; the finite 2023 fee/tax Bands are not edited or reinterpreted.

Only after F1 passes may a pure target-scoped projection reuse the existing finite Kernel RuleBook types, followed by a new additive five-dimension declaration and G12C/D v2 publication. Builder publication must not import Kernel. Only after that publication passes may the G12H report/failure contract and RED matrix be frozen against its manifest/events.

G12H must consume this declaration; it must not invent dimensions from `ProfilePortType` or enumerate Kernel classes.

## Provisional implementation seam

The approved staged seam is frozen in the v2 plan: F2, if still needed after real evidence passes, is one concrete pure function beside existing Kernel A-share fee/tax types; F3 is one internal canonical Builder publication function with no Kernel/Runtime import or root export. No Runtime/profile composer, source projector, generic rule framework, or analyzer implementation is added before its prerequisite phase passes. The current publication remains valid source evidence for a deterministic gap, not a success fixture.

## Readiness checks

Before changing G12H `readiness` to `READY`, F1-F3 evidence must prove:

- exact profile/declaration/Bundle binding;
- one canonical authority body for each fixture dimension, without mandating an acquisition transport schema;
- complete half-open interval coverage and unique resolution;
- gap/overlap/source-identity failures and deterministic precedence;
- explicit zero-dimension behavior;
- no Runtime/Kernel production import or second rule engine;
- repeat canonical hashes and immutable fixture authority.

## Non-goals

No generic rule model, DSL, registry, evaluator, economic calculation, provider lookup, fallback, imputation, Runtime boot branch, decision-grade claim, or deployment authorization.
