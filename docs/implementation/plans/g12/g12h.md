---
id: G12H
proposed_readiness: SEE_PARENT_ACCEPTANCE_MATRIX
registry_status: SEE_PARENT_ACCEPTANCE_MATRIX
owner: market-bundle-builder validation
produces:
  - RuleCoverageReport
consumes:
  - G12C MarketBundleManifest
  - future immutable profile rule-coverage declaration
depends_on:
  contract: [G12C, G08E-ROUTE-PRODUCT-FEE-V2C]
  evidence: [real profile rule-dimension declaration, normalized typed rule-source fixture]
  write_conflict: [builder-root-exports, builder-coverage-policy, acceptance-registry]
fan_out: [G12L-*, G12M-*]
---

# G12H Rule Coverage

## Outcome

Eventually prove complete, non-overlapping historical coverage for every rule dimension explicitly required by one immutable market-profile declaration, without duplicating Kernel economics.

## Current status

The parent [Acceptance Matrix](../../acceptance-matrix.md) is the sole current Gate-status authority. Strict F1 predecessor/endpoint/successor authority closure remains blocked. The immutable v1 development Bundle at `832f53a74d3f74436ecae8672bd1c0dd3530c814` still deterministically produces `COVERAGE_GAP / market_fees`. Separately, the ADR-0007 current-selected development snapshot, finite v2 RuleBooks, and five-dimension G12C/D publication passed at `cdc5a29133bbc0b863ec409219fb50bd0b299c77`; D4 finite development coverage passed at `0215ed3a369bff10a64830c462c569b378914670` without changing any strict-path qualification.

[ADR 0004](../../../adr/0004-official-rules-effective-until-authoritatively-superseded.md) approves additive effective-until-authoritatively-superseded semantics and retrospective successor closure without backdating evidence. [ADR 0005](../../../adr/0005-cn-a-share-fees-require-access-route-and-product-class.md) accepts explicit execution access route plus fee product class. [ADR 0006](../../../adr/0006-explicit-official-live-status-may-use-receipt-time.md) permits a verified receipt time to date an explicit nonblank issuer-owned official live-status API response. [ADR 0007](../../../adr/0007-current-official-selection-supports-development-projection.md) separately permits current official selections to support a finite development projection while keeping strict successor closure and every stronger qualification false. The parent Acceptance Matrix records pure Kernel V2A, Runtime V2B, and fan-in V2C `PASSED`. The strict [effective-until-superseded plan](g12h-effective-until-superseded-v2.md) remains F1-blocked; the additive [current-selected development plan](g12h-current-selected-development-v1.md) has passed D1-D4. Its complete finite interval report does not set `rule_coverage_qualified=true` or establish legal/history completeness.

Research authority:

- `docs/research/g12h-rule-coverage.md`;
- `docs/research/g12h-five-dimension-target-coverage-blocker-v1.md`;
- `docs/research/g12h-xshe-july-2026-fee-tax-authority-primary-sources.md`;
- `docs/research/g12h-xshe-july-2026-full-envelope-successor-closure-f1.md`;
- `docs/research/g12h-competent-status-register-discovery-v1.md`;
- `docs/research/g12h-live-status-relaxation-impact-v1.md`;
- `docs/research/g12h-live-status-api-probes-v1.md`;
- `docs/research/g12h-current-selected-development-authority-v1.md`.

## Blocker to resolve first

Parent-Matrix [G08E-V2C acceptance](../g08/g08e-route-product-fee-v2-acceptance.md) is `PASSED`; V2A freezes pure Kernel facts, V2B owns Runtime profile/build and Semantic Run binding, and V2C fans them in. Its acceptance uses only finite XSHE v1-to-v2 compatibility economics and therefore does not depend on G12H F1. G12H F1 may continue only for the exact execution-enforceable `DOMESTIC + ORDINARY_A_SHARE` scope: XSHE, `InstrumentType.EQUITY`, CNY quote/settlement, `AUCTION`, all boards admitted by the immutable bound profile, and `trade_notional` basis. F1 must close five ordered lineages—handling, regulatory, ChinaClear transfer, HKSCC transfer as explicitly not applicable to the domestic route with a nonempty authority ref, and stamp duty—while enforcing `target_to_exclusive <= official_record_as_of <= closure_evidence_available_at`. ADR 0006 cures the missing record-state timestamp only for the NPC Stamp Duty Law's explicit `有效` result. The bounded API probes additionally freeze SZSE's exact current-document JSON selector and prove that No. 39 is unclassified under every published STA status filter; neither is an explicit nonblank validity result, and the other lineages still lack exact-corpus complete successor/correction channels. Existing PASSED G08E/G08H and publication artifacts remain immutable; finite 2023 v1 fee/tax Bands are not edited, reinterpreted, or extrapolated.

Only after F1 passes may the strict path claim official successor closure. Independently, ADR 0007 permits the current-selected development path to construct finite route/product v2 RuleBooks and a new five-dimension G12C/D development publication without setting `official_record_as_of` or changing any strict-path qualification. Builder publication must not import Kernel. Only after that development publication passes may the G12H report/failure contract be frozen against its manifest/events with coverage completeness explicitly separated from legal/history completeness.

G12H must consume this declaration; it must not invent dimensions from `ProfilePortType` or enumerate Kernel classes.

## Provisional implementation seam

The approved staged seam is frozen in the plans: V2A adds only one concrete Kernel fee module; V2B owns private Runtime profile/order selection and additive v2 profile/build/Semantic Run binding; V2C records acceptance in the parent Matrix. The strict path may add its pure source projector only after real closure. The development path reuses existing v2 value types, freezes one canonical snapshot/declaration, and adds one off-root Builder publication function with no Kernel/Runtime import or root export. No generic rule framework, Runtime market branch, or analyzer implementation is added before its prerequisite phase passes. The v1 publication remains immutable evidence of the original deterministic gap.

## Readiness checks

Before changing G12H `readiness` to `READY`, F1-F3 evidence must prove:

- exact profile/declaration/Bundle binding;
- one canonical authority body for each fixture dimension, without mandating an acquisition transport schema;
- terminal documentary correction selection followed by economic ordering at authoritative effective times;
- exact `DOMESTIC + ORDINARY_A_SHARE` execution Scope (account/Venue/Instrument/type/CNY/AUCTION/side context), bound-profile board scope, trade-notional basis, retrospective cutoff invariant, and nonempty separate ChinaClear/HKSCC source refs;
- complete half-open interval coverage and unique resolution;
- closure-only versus target-affecting identity behavior;
- gap/overlap/fork/conflict/source-identity failures and deterministic precedence;
- explicit zero-dimension behavior;
- no Runtime/Kernel production import or second rule engine;
- repeat canonical hashes and immutable fixture authority.

## Non-goals

No generic rule model, DSL, registry, evaluator, economic calculation, provider lookup, fallback, imputation, Runtime boot branch, decision-grade claim, or deployment authorization.
