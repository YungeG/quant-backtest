---
id: G12H
readiness: BLOCKED
gate_status: DRAFT
owner: market-bundle-builder validation
produces:
  - RuleCoverageReport
consumes:
  - G12C MarketBundleManifest
  - future immutable profile rule-coverage declaration
depends_on:
  contract: [G12C]
  evidence: [real profile rule-dimension declaration, normalized typed rule-source fixture]
  write_conflict: [builder-root-exports, builder-coverage-policy, acceptance-registry]
fan_out: [G12L-*, G12M-*]
---

# G12H Rule Coverage

## Outcome

Eventually prove complete, non-overlapping historical coverage for every rule dimension explicitly required by one immutable market-profile declaration, without duplicating Kernel economics.

## Current status

`DRAFT / BLOCKED`. The exact development-only prerequisite Bundle is `PASSED` at `832f53a74d3f74436ecae8672bd1c0dd3530c814`, but it proves that the five required dimensions have no common target coverage. Calendar, order rules, and corporate-action entitlements exact-cover July 2026; market fees and stamp duty stop in August 2023. Primary-source research frozen at `c95952a862057faf36c87ef14839da4105315cb8` identifies candidate later rates but cannot prove finite full-target continuity/revision closure. The target also extends `10 days 6 hours` beyond the immutable analysis instant, so open-ended rules cannot cover that segment without inferring no change. The deterministic earliest result remains `COVERAGE_GAP / market_fees`; no success contract or analyzer implementation is authorized.

Research authority:

- `docs/research/g12h-rule-coverage.md`;
- `docs/research/g12h-five-dimension-target-coverage-blocker-v1.md`;
- `docs/research/g12h-xshe-july-2026-fee-tax-authority-primary-sources.md`.

## Blocker to resolve first

First freeze one additive, versioned five-dimension authority set whose Calendar, order rules, market fees, stamp duty, and corporate-action entitlements all exact-cover one common finite target and are available under an explicitly accepted analysis/closure contract. Existing PASSED G08H and publication artifacts remain immutable; the finite 2023 fee/tax bands may not be extrapolated to 2026. For the current target, acceptable paths are limited to pre-analysis primary authority that itself fixes validity through target end, or an explicit upstream profile/build-owner decision changing retrospective-closure or target/analysis semantics in a new additive declaration and publication.

Only after that prerequisite passes may the G12H declaration/report/failure contract and RED matrix be frozen against its manifest/events.

G12H must consume this declaration; it must not invent dimensions from `ProfilePortType` or enumerate Kernel classes.

## Provisional implementation seam

Only after an aligned five-dimension prerequisite is PASSED may Builder add a pure atomic validator producing one `RuleCoverageReport` or one `RuleCoverageFailure`. The current publication is valid source evidence for a deterministic gap, not a success fixture. Final public names, canonical bodies, failure codes, and RED matrix remain unfrozen.

## Readiness checks

Before changing `readiness` to `READY`, evidence must prove:

- exact profile/declaration/Bundle binding;
- one normalized source schema for each fixture dimension;
- complete half-open interval coverage and unique resolution;
- gap/overlap/source-identity failures and deterministic precedence;
- explicit zero-dimension behavior;
- no Runtime/Kernel production import or second rule engine;
- repeat canonical hashes and immutable fixture authority.

## Non-goals

No generic rule model, DSL, registry, evaluator, economic calculation, provider lookup, fallback, imputation, Runtime boot branch, decision-grade claim, or deployment authorization.
