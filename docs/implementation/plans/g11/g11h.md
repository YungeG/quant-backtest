---
id: G11H
owner: backtest-runtime strategy
produces:
  - ModelArtifactRef
  - ModelRevisionTimeline
consumes:
  - G11B point-in-time causality
  - G11F StrategyState
depends_on:
  contract: [G11B, G11F]
  evidence: [walk-forward-model-revision-fixture]
  write_conflict: [runtime-root-exports, acceptance-registry]
fan_in: G11I
---

# G11H ModelArtifact and Revision Timeline

## Outcome

Immutable point-in-time model lookup and Walk-forward revision switching whose selected artifact identity enters StrategyState and Decision evidence.

## Readiness work

Freeze model, training-data, training-code and feature-schema identity; full Simulation Instant availability; revision-chain selection; and switch evidence.

## Dependencies

Research may proceed after the G11B contract freeze. Implementation waits for G11B and G11F PASSED. Target `model_revisions.py`.

## Exclusions

Runtime training, tuning, candidate ranking, experiment selection, and mutable model registries remain outside the Runtime.
