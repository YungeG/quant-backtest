---
id: G11D
owner: backtest-runtime observations
produces:
  - NamedBarWindowView
consumes:
  - G11B PointInTimeObservationView
  - BarDefinition contracts
depends_on:
  contract: [G11A, G11B, BarDefinition]
  evidence: [named-bar-window-coverage-fixture]
  write_conflict: [runtime-root-exports, acceptance-registry]
fan_in: G11E
---

# G11D Named Bar and Window Access

## Outcome

Versioned named Bar Stream queries with bounded lookback and explicit coverage results, using only bars visible at the Decision Simulation Instant.

## Readiness work

Research authority: `docs/research/g11d-named-bar-window.md`.

The detailed Gate contract is authoritative in the G11D Acceptance Card in `docs/implementation/acceptance-matrix.md`; this plan does not duplicate status.

Freeze:

- BarDefinition key/version authority;
- bounded window request and deterministic ordering;
- partial/missing lookback failure semantics;
- no unaudited resampling or post-decision fields;
- one finite Bar/window fixture.

## Dependencies

G11B is PASSED. The G11D card freezes the minimal `BarDefinitionRef` identity needed for already-aggregated streams. Prefer `observation_windows.py` to avoid G11C write conflicts.

## Exclusions

Universe construction, Decision scheduling, indicator libraries, arbitrary resampling, and G12 gap qualification remain outside this Gate.
