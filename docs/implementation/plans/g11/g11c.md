---
id: G11C
owner: backtest-runtime observations
produces:
  - PointInTimeUniverseView
consumes:
  - G11B PointInTimeObservationView
depends_on:
  contract: [G11A, G11B]
  evidence: [listing-delisting-membership-fixture]
  write_conflict: [runtime-root-exports, acceptance-registry]
fan_in: G11I
---

# G11C Point-in-time Universe

## Outcome

A typed point-in-time Universe query for listing, delisting, membership, and explicitly labelled StaticUniverse evidence without filesystem discovery or survivorship claims.

## Readiness work

Research authority: `docs/research/g11c-point-in-time-universe.md`.

The detailed Gate contract is authoritative in the G11C Acceptance Card in `docs/implementation/acceptance-matrix.md`; this plan does not duplicate status.

Freeze:

- Universe query identity and result shape;
- listing/delisting and membership effective/available-time rules;
- StaticUniverse limitations;
- failure precedence for missing, conflicting, future, or incomplete evidence;
- one finite fixture with membership changes.

## Dependencies

G11B is PASSED. Prefer a separate `universe.py` deep module so G11D can proceed without shared-file writes.

## Exclusions

Bar/window access, scheduling, Strategy invocation, directory scanning, and G12 completeness remain outside this Gate.
