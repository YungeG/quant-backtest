---
id: G11F
owner: backtest-runtime strategy
produces:
  - StrategyState
  - StrategyCheckpoint
consumes:
  - canonical domain identity and serialization
depends_on:
  contract: [G02]
  evidence: [strategy-state-restore-fixture]
  write_conflict: [runtime-root-exports, acceptance-registry]
fan_in: [G11G, G11H, G11I]
---

# G11F StrategyState and Checkpoint

## Outcome

Canonical Strategy-owned business state with before/after hashes and deterministic checkpoint restore, distinct from EngineCheckpoint and financial account state.

## Readiness work

Freeze:

- allowed canonical state values and schema versioning;
- before/after transition evidence;
- checkpoint identity and restore parity;
- rejection of hidden filesystem/network/wall-clock/cache state;
- one continuation-after-restore fixture.

## Parallel position

Its contract dependency is already PASSED, so research/readiness can proceed independently of G11B. Implementation uses a separate `strategy_state.py` seam under the single-writer queue.

## Exclusions

Inventory, Ledger, model artifacts, RNG streams, scheduling, and Engine recovery are separate authorities.
