---
id: G11E
owner: backtest-runtime strategy
produces:
  - DecisionSchedule
  - WarmupEligibility
consumes:
  - G11B decision instant semantics
  - G11D window coverage
depends_on:
  contract: [G11B, G11D]
  evidence: [schedule-warmup-fixture]
  write_conflict: [runtime-root-exports, acceptance-registry]
fan_in: G11I
---

# G11E DecisionSchedule and Warmup

## Outcome

A deterministic Decision schedule that separates Warmup from the half-open Active interval and proves lookback eligibility before invoking Strategy code.

## Readiness work

Freeze schedule identity, trigger ordering, Warmup/Active boundaries, lookback requirements, and atomic same-instant invocation windows.

## Dependencies

Blocked on G11D. Target a separate `decision_schedule.py` module.

## Exclusions

Warmup cannot produce Orders, Fills, Journal entries, or performance. StrategyState, RNG, model lookup, and invocation remain G11F–I.
