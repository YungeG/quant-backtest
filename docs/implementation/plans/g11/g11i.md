---
id: G11I
owner: backtest-runtime strategy
produces:
  - PortfolioStrategyInvocation
  - atomic DecisionBatch handoff
consumes:
  - G11A-G11H Strategy Runtime contracts
  - G04 Validator and DecisionBatch
depends_on:
  contract: [G11A, G11B, G11C, G11D, G11E, G11F, G11G, G11H, G04]
  evidence: [portfolio-strategy-invocation-fixture]
  write_conflict: [runtime-integration, runtime-root-exports, acceptance-registry]
fan_in: G11J
---

# G11I PortfolioStrategy Invocation and DecisionBatch

## Outcome

The G11 fan-in: deterministic Strategy invocation, complete observation/state/RNG/model evidence, Candidate validation, and all-or-nothing handoff to the existing atomic DecisionBatch path.

## Readiness work

Research authority: `docs/research/g11i-portfolio-strategy-invocation.md`.

The detailed Gate contract and PASSED evidence are authoritative in the G11I Acceptance Card in `docs/implementation/acceptance-matrix.md`; this plan does not duplicate status.

The frozen seam uses one offline `strategy_runtime.py` orchestrator, canonical registration order, immutable least-authority callback contexts, complete attempted state/RNG evidence, existing `StrategyOutputValidator`, and exactly one existing `AtomicDecisionBatchCollector` call on eligible Active success.

G11E legal same-UTC full-instant continuation is supported by an additive exact-`SimulationInstant` v2 identity path. Legacy G04 constructors, canonical bytes, fixtures, and v1 hashes remain unchanged; exact/legacy ambiguity fails closed.

## Dependencies

G11A–G11H and G04 are PASSED. G11I is implemented at `strategy_runtime.py`; Engine, Runner, Timeline, and TargetStream have no G11I-specific branch.

## Exclusions

Strategy Runtime cannot bypass Allocation, Risk, Planning, Execution, Accounting, or canonical Runner evidence. It does not provide dynamic Strategy loading, process/thread execution, ambient-library sandboxing, provider access, decision-grade qualification, or deployment authorization.
