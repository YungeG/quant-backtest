---
id: G11G
owner: backtest-runtime strategy
produces:
  - NamedRandomStream
consumes:
  - G11F StrategyState/checkpoint
depends_on:
  contract: [G11F]
  evidence: [random-stream-isolation-fixture]
  write_conflict: [runtime-root-exports, acceptance-registry]
fan_in: G11I
---

# G11G Named Random Streams

## Outcome

Versioned counter-based Strategy-specific random streams derived from the master seed without global RNG coupling.

## Readiness work

Research authority: `docs/research/g11g-named-random-streams.md`.

The detailed Gate contract is authoritative in the G11G Acceptance Card in `docs/implementation/acceptance-matrix.md`; this plan does not duplicate status.

Freeze stream key/version/algorithm/counter identity, derivation rules, checkpoint integration, and isolation from unrelated component draws.

## Dependencies

The G11F contract dependency is PASSED. Target a separate `random_streams.py` module.

## Exclusions

Parameter search, model selection, nondeterministic entropy, and shared process-global RNG remain forbidden.
